# Conflict-copy review log — one row per host per day

> Gate for the daily conflict-copy sweep (PROTOCOL rule R7). File-sync
> providers create conflict copies on concurrent edits; find them (patterns
> like `*conflict*` or host-suffixed duplicates), merge, remove, log here.

| date | host | conflicts found | resolution |
|---|---|---|---|

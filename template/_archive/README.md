# _archive/ — integrated and expired items

Nothing in the yard gets raw-deleted (except read messages): once an item is
integrated everywhere it applies, it moves here. Cheap storage, expensive
reconstructions — the archive is the audit trail of what traveled between
your systems.

Suggested layout: keep the original filename; prefix with the archive date if
names would collide (`2026-07-11__<original-name>`).
